class User {
    constructor(email, pseudo, password, id) {
        this.email = email;
        this.pseudo = pseudo;
        this.password = password;
        this.id = id;
    }
}

module.exports = User;